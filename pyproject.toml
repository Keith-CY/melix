[project]
name = "melix-workspace"
version = "0.1.0"
description = "Workspace metadata for Melix."
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["services/mlx-worker-python"]
