# M6.7 KV-Cache Quantization Acceleration

## Goal

Add feature-flagged KV-cache quantization acceleration so memory pressure can be reduced on long-running decode paths with measurable trade-offs.

## Scope

- add runtime policy for KV-cache quantization acceleration
- preserve correctness-first behavior for the default path
- expose metrics for memory reduction and throughput impact

## Files

- update `services/mlx-text-worker-swift/Sources/Core/Inference/`
- update `services/mlx-text-worker-swift/Sources/Core/Runtime/`
- update `services/control-plane-swift/Sources/ModelCatalog/`
- update `apps/macos-menubar/Sources/AppMain/`

## Implementation Notes

- acceleration should remain opt-in and benchmarked
- runtime metrics must distinguish active-path acceleration from storage-boundary quantization
- keep the policy surface compatible with per-model acceleration settings
- probe coverage must land before any TurboQuant kernel or cache optimization work
- the pre-optimization affine q4 baseline must be captured from the same report command used for post-optimization comparison
- active-KV reports should include backend, kernel-path, model-call timing, quantization timing, estimated KV bytes, and memory-savings probes
- real-model helper code must reuse the repository's Phase 8 small-text-model convention instead of duplicating Qwen3.5 fixture resolution
- the shared real-model helper should keep deterministic test fixtures local, while allowing benchmark commands to resolve an existing local Hugging Face cache or managed model path before falling back to the Hub id
- Phase 8 real-model E2E uses `mlx-community/Qwen3.5-0.8B-OptiQ-4bit`; Melix now vendors the Qwen3.5 Swift model classes from `mlx-swift-lm` 2.31.3 so the same real model can load and decode in the Swift stack
- live Swift MLX model probes must run with a metallib matching the Swift MLX dependency; `MELIX_SWIFT_MLX_METALLIB_PATH` should point at an `mlx_metal` 0.29.1 `mlx.metallib` matching `mlx-swift` 0.29.1 when the Python worker environment carries a newer MLX wheel
- the first post-baseline optimization removes redundant decode-side quantization maintenance calls once the cache is already quantized; the post-run evidence is `docs/metrics/phase2-active-kv-decode-guard-postopt.json`
- `turboquant-q4` must be treated as a release-gated fused profile: it may report a non-fallback kernel path only after model attention actually dispatches the fused decode route, and the release gate must remain failed until real-model throughput overhead is at or below 15 percent
- fallback `turboquant-q4` probes must fail the explicit fused release gate in `swift_worker_direct.active_kv_release_gates.turboquant_fused_decode`; routed probes still fail when `worker_tps_overhead_pct > 15`
- the report CLI supports `--require-fused-turboquant`; use it for fused-kernel candidates, not for the current fallback post-run
- architecture notes for the fused-kernel route live in `docs/architecture/2026-04-18-turboquant-kv-cache-optimization.md`
- the Swift custom Metal feasibility slice is covered by `TurboQuantMetalKernelCapability.runIdentitySmokeKernel(...)`, `TurboQuantMetalKernelCapability.runMSEQ4ValueDecodeSmokeKernel(...)`, `TurboQuantMetalKernelCapability.runMSEQ4FusedAttentionSmokeKernel(...)`, `TurboQuantMetalKernelCapability.runMSEQ4FusedAttentionKernelFromQuantizedState(...)`, `WorkerScaffoldTests.testTurboQuantMetalCapabilityRunsCustomIdentityKernel`, `WorkerScaffoldTests.testTurboQuantMetalCapabilityRunsMSEQ4ValueDecodeKernel`, `WorkerScaffoldTests.testTurboQuantMetalCapabilityRunsMSEQ4FusedAttentionKernel`, `WorkerScaffoldTests.testTurboQuantMetalCapabilityRunsMSEQ4FusedAttentionFromQuantizedKVCacheState`, and `WorkerScaffoldTests.testTurboQuantMetalCapabilityRejectsUnsupportedQuantizedKVCacheStateInputs`; it proves `MLXFast.metalKernel(...)` can compile and dispatch identity, packed-q4 value decode, and one-dispatch packed-q4 score plus softmax plus value work in the text worker target before any TurboQuant runtime path is changed
- `swift_worker_direct.active_kv_fused_candidate_probes.turboquant_q4` records smoke capability separately from runtime evidence and must not be used as release-gate proof by itself
- the second runtime candidate dispatches a fused MSE q4 Metal kernel from live MLXLM `QuantizedKVCache` state when available, falls back to the fixed smoke arrays otherwise, and reports `active_kv_candidate_dispatch_code = 1` after either candidate dispatch runs; it is supporting evidence only
- Melix now vendors `mlx-swift-lm` in `third_party/mlx-swift-lm` and patches `MLXLMCommon.attentionWithCacheUpdate(...)` to try `fusedQ4ScaledDotProductAttention(...)` before upstream affine q4 quantized SDPA for supported one-token q4 affine decode states
- `WorkerScaffoldTests.testTurboQuantRuntimeRouteReportsRoutedAfterFusedAttentionDispatch`, `WorkerScaffoldTests.testVendoredFusedQ4AttentionMatchesQuantizedReferenceForDecodeGQA`, `WorkerScaffoldTests.testAttentionWithCacheUpdateUsesFusedQuantizedStorageWithoutMaterializingForDecode`, and `WorkerScaffoldTests.testAutoSwiftMLXBackendDecodeReportsTurboQuantFusedRuntimeRoute` cover runtime route promotion only after a real fused attention dispatch counter increments; the storage-fastpath test also proves the routed fused path can consume overallocated quantized storage with multiple KV heads without per-token trimmed-state materialization
- Swift worker metrics export `active_kv_runtime_route` and `active_kv_runtime_block_reason` so release-gate evidence can distinguish unsupported cache state, blocked fallback, and actual fused routing
- default Swift runtime keeps fused TurboQuant candidate dispatch disabled unless `MELIX_SWIFT_TURBOQUANT_CANDIDATE_PROBE=1` is set before `scripts/dev_up.sh`; runtime route evidence comes from the vendored attention dispatch counter, not from candidate probes
- the first runtime speedup evidence is `docs/metrics/phase2-active-kv-runtime-speedup-postopt.json`; it proves the default path has `candidate_dispatch_count = 0`, while the release gate stays failed because `active_kv_kernel_path = fallback` and `worker_tps_overhead_pct = 45.76`
- the candidate-check speedup evidence is `docs/metrics/phase2-active-kv-candidate-check-probe-preopt.json` and `docs/metrics/phase2-active-kv-candidate-check-postopt.json`; on the supported live `mlx-community/Qwen3-0.6B-4bit` probe, default `turboquant-q4` candidate eligibility checks dropped from 320 to 0 across five 64-token repeats, while the fused release gate remained failed because `active_kv_kernel_path = fallback` and `worker_tps_overhead_pct = 42.62`
- the terminal model-call cleanup evidence is `docs/metrics/phase2-active-kv-terminal-model-call-preopt.json` and `docs/metrics/phase2-active-kv-terminal-model-call-postopt.json`; the probe adds `active_kv_decode_model_call_count` and the optimization stops prepared decode before computing logits for a token that cannot be emitted after `maxOutputTokens`, dropping default `turboquant-q4` decode model calls from 320 to 315 across five 64-token repeats while the fused release gate remains failed because `active_kv_kernel_path = fallback` and `worker_tps_overhead_pct = 43.55`
- the lazy-eval timing evidence is `docs/metrics/phase2-active-kv-lazy-eval-probe.json`; it adds `active_kv_decode_token_eval_total_us`, `active_kv_decode_token_eval_call_count`, `active_kv_decode_token_eval_avg_us`, and `active_kv_decode_loop_total_us` so Swift MLX lazy graph construction can be separated from the sampling/token materialization step that forces execution. The real-model run reports default `turboquant-q4` release-gate `decode_token_eval_total_us = 6438631`, `decode_loop_total_us = 9497000`, `worker_tps_overhead_pct = 42.37`, and `active_kv_kernel_path = fallback`, so the fused release gate remains blocked.
- the next non-hook runtime speedup keeps `q4` available for explicit affine KV compression, but prevents default `turboquant-q4` from silently paying the affine q4 fallback decode cost while the attention hook is unavailable. Candidate-audit runs keep the old quantized fallback by setting `MELIX_SWIFT_TURBOQUANT_CANDIDATE_PROBE=1`; developers can also set `MELIX_SWIFT_TURBOQUANT_AFFINE_FALLBACK=1` for compatibility probes. The fused release gate must still fail until `active_kv_kernel_path != fallback`, `active_kv_estimated_memory_savings_pct >= 67`, and `worker_tps_overhead_pct <= 15` all hold in the same real-model JSON.
- the blocked-fallback speedup evidence is `docs/metrics/phase2-active-kv-blocked-fallback-speedup-postopt.json`; default `turboquant-q4` no longer quantizes the cache while the attention hook is unavailable, so the real-model run reports `active_kv_quantization_ratio = 0`, `active_kv_estimated_memory_savings_pct = 0.0`, `decode_token_eval_total_us = 4009440`, `decode_loop_total_us = 5473976`, and `worker_tps_overhead_pct = -3.39`. The fused release gate remains failed because `active_kv_kernel_path = fallback`, `active_kv_runtime_route = blocked`, and memory savings are zero.
- GitHub issue [#35](https://github.com/Keith-CY/melix/issues/35) tracks the deferred upstream attention-hook alternative; do not unblock the fused active-KV release gate until a real-model metrics JSON shows both `worker_tps_overhead_pct <= 15` and `active_kv_kernel_path != fallback`
- the vendored runtime evidence is `docs/metrics/phase2-active-kv-vendored-turboquant-runtime.json`; with `mlx-community/Qwen3-0.6B-4bit`, matching Swift MLX 0.29.1 metallib, and no `MELIX_SWIFT_TURBOQUANT_CANDIDATE_PROBE`, it reports `active_kv_kernel_path = tq_mse_single`, `active_kv_runtime_route = routed`, `active_kv_fallback_count = 0`, `active_kv_decode_quantize_total_us = 0`, `active_kv_estimated_memory_savings_pct = 75.0`, and `candidate_dispatch_count = 0`, but the release gate remains failed because `worker_tps_overhead_pct = 68.63`
- the earlier vendored-runtime probe attempted the repository's Phase 8 Qwen3.5 small-model convention first, but direct Swift MLX load failed with `Unsupported model type: qwen3_5`; that registry/runtime blocker is now removed by the vendored Qwen3.5 support
- the shared-score speedup evidence is `docs/metrics/phase2-active-kv-vendored-turboquant-shared-scores.json`; it keeps `active_kv_kernel_path = tq_mse_single`, `active_kv_runtime_route = routed`, `active_kv_fallback_count = 0`, `active_kv_decode_quantize_total_us = 0`, `active_kv_estimated_memory_savings_pct = 75.0`, and `candidate_dispatch_count = 0`, and improves `worker_tps_overhead_pct` from `68.63` to `60.87`; the release gate remains failed because the threshold is `<= 15`
- the online-softmax speedup evidence is `docs/metrics/phase2-active-kv-vendored-turboquant-online-softmax.json`; it replaces the shared-score vector materialization with a single-simdgroup online-softmax recurrence and keeps `active_kv_kernel_path = tq_mse_single`, `active_kv_runtime_route = routed`, `active_kv_fallback_count = 0`, `active_kv_decode_quantize_total_us = 0`, `active_kv_estimated_memory_savings_pct = 75.0`, and `candidate_dispatch_count = 0`. It improves `decode_token_eval_total_us` versus the intermediate online-softmax layout, but the release gate remains failed because `worker_tps_overhead_pct = 60.0`, still above the `<= 15` threshold.
- the packed-word lane speedup evidence is `docs/metrics/phase2-active-kv-vendored-turboquant-packed-word-lanes.json`; it assigns each active lane to one packed q4 `uint32` word, dequantizes eight values once per token, reuses that load for both score contribution and eight value accumulators, and keeps `groupSize` constrained to q4-word-aligned groups. It keeps `active_kv_kernel_path = tq_mse_single`, `active_kv_runtime_route = routed`, `active_kv_fallback_count = 0`, `active_kv_decode_quantize_total_us = 0`, `active_kv_estimated_memory_savings_pct = 75.0`, and `candidate_dispatch_count = 0`; `decode_token_eval_total_us` improves from `6601404` to `2554186` versus the online-softmax run, but the release gate remains failed because the same-run `worker_tps_overhead_pct = 73.97`, above the `<= 15` threshold.
- Qwen3.5 support evidence is `docs/metrics/phase2-active-kv-qwen35-support-smoke.json`; with the real `mlx-community/Qwen3.5-0.8B-OptiQ-4bit` snapshot it proves Swift stack load and decode, but `turboquant-q4` remains release-blocked for this model because `active_kv_kernel_path = fallback`, `active_kv_runtime_route = blocked`, `active_kv_runtime_block_reason = unsupported_cache_state`, `active_kv_fallback_count = 1`, and `active_kv_estimated_memory_savings_pct = 0.0`. Do not use Qwen3.5 as fused release-gate pass evidence until hybrid cache handling produces a same-model JSON with `active_kv_kernel_path != fallback`.
- the fused eval-sync probe evidence is `docs/metrics/phase2-active-kv-vendored-turboquant-eval-probe.json`; with the real `mlx-community/Qwen3.5-0.8B-OptiQ-4bit` snapshot and `MELIX_SWIFT_ACTIVE_KV_FORCE_MODEL_EVAL_PROBE=1`, it keeps the release gate failed because `active_kv_kernel_path = fallback`, `active_kv_runtime_route = blocked`, `active_kv_runtime_block_reason = unsupported_cache_state`, `active_kv_fallback_count = 3`, and `active_kv_estimated_memory_savings_pct = 0.0`. The probe reports `decode_model_eval_sync_total_us = 4311359`, `decode_model_eval_sync_call_count = 189`, comparison median `active_kv_decode_model_eval_sync_avg_us = 22607`, and `active_kv_fused_attention_call_count = 0`, so it measures lazy model-eval sync cost without proving fused Qwen3.5 routing.
- the Qwen3.5 hybrid-cache routed evidence is `docs/metrics/phase2-active-kv-qwen35-hybrid-turboquant-routing.json`; it changes dynamic KV quantization from a first-cache guard to per-layer scanning, leaving Qwen3.5 `MambaCache` entries unchanged while quantizing supported full-attention `KVCacheSimple` entries. The same real-model JSON reports `active_kv_kernel_path = tq_mse_single`, `active_kv_runtime_route = routed`, `active_kv_fallback_count = 0`, `active_kv_estimated_memory_savings_pct = 75.0`, `fused_attention_call_count = 1134`, `worker_tps_overhead_pct = 12.82`, and release-gate `status = pass`, so it unblocks the first fused TurboQuant release gate.
- the Qwen3.5 hybrid-cache stability summary is `docs/metrics/phase2-active-kv-qwen35-hybrid-stability-summary.json`; it preserves five sequential same-model `turboquant-q4` runs. All five pass `--require-fused-turboquant`, all five keep `active_kv_kernel_path = tq_mse_single`, `active_kv_runtime_route = routed`, `active_kv_fallback_count = 0`, and `fused_attention_call_count = 1134`; `worker_tps_overhead_pct` min/median/mean/max is `5.71 / 10.26 / 9.722 / 12.82`, so the repeated-run evidence stays below the `<= 15` first fused milestone threshold.
- the Qwen3.5 TurboQuant speedup evidence is `docs/metrics/phase2-active-kv-qwen35-turboquant-speedup-preopt.json` and `docs/metrics/phase2-active-kv-qwen35-turboquant-speedup-postopt.json`; the pre-optimization lane probe disproves launch-width waste for Qwen3.5 because active lanes and launched lanes are both `12096`, and the post-optimization kernel computes online-softmax state on one lane, broadcasts it to value lanes, and hoists each lane's eight query values out of the historical-token loop. The post-run keeps `active_kv_kernel_path = tq_mse_single`, `active_kv_runtime_route = routed`, `active_kv_fallback_count = 0`, reduces softmax token-lane work from `641088` to `20034`, and reports release-gate `status = pass` with `worker_tps_overhead_pct = 5.13`. Treat the TPS delta as noisy single-run evidence; the softmax token-lane reduction is deterministic kernel work reduction.
- the Qwen3.5 TurboQuant speedup stability summary is `docs/metrics/phase2-active-kv-qwen35-turboquant-speedup-stability-summary.json`; it reruns one warmup plus five measured same-model `turboquant-q4` probes with `MELIX_SWIFT_ACTIVE_KV_FORCE_MODEL_EVAL_PROBE=1`. The warmup is recorded but excluded from aggregate release evidence. All five measured runs pass `--require-fused-turboquant`, keep `active_kv_kernel_path = tq_mse_single`, `active_kv_runtime_route = routed`, `active_kv_fallback_count = 0`, and aggregate softmax token-lane work `60102` per run. `worker_tps_overhead_pct` min/median/mean/max is `10.0 / 10.26 / 11.044 / 12.5`, so the speedup has repeated-run release-gate pass evidence under the warmup protocol.
- the cache-internal timing probe evidence is `docs/metrics/phase2-active-kv-vendored-turboquant-cache-probe.json`; it adds `active_kv_cache_update_*`, `active_kv_cache_expand_total_us`, `active_kv_cache_quantize_total_us`, `active_kv_cache_append_total_us`, and `active_kv_cache_materialize_*` to Swift metrics, decode rows, comparisons, release-gate evidence, and fused-candidate runtime evidence. The probe splits `QuantizedKVCache.updateQuantized(...)` into update total, storage expansion/init, incoming KV quantization, storage append, and trimmed-state materialization, and it also counts `getQuantizedState()` materialization. The real-model run keeps `active_kv_kernel_path = tq_mse_single`, `active_kv_runtime_route = routed`, and `active_kv_fallback_count = 0`, while reporting `cache_update_total_us = 1081676`, `cache_append_total_us = 621474`, `cache_materialize_total_us = 348621`, and `worker_tps_overhead_pct = 73.61`; the release gate remains failed.
- the storage-fastpath speedup evidence is `docs/metrics/phase2-active-kv-vendored-turboquant-storage-fastpath.json`; it removes decode-side trimmed quantized-state materialization from the routed fused path. `QuantizedKVCache.updateQuantizedStorage(...)` returns full preallocated q4 storage plus the effective sequence length; `attentionWithCacheUpdate(...)` tries the fused q4 route against that storage state and only materializes with `getQuantizedState()` when the fused route is unavailable. The fused Metal route now uses the storage sequence length as the memory stride and the effective sequence length as the softmax loop bound. The real-model run keeps `active_kv_kernel_path = tq_mse_single`, `active_kv_runtime_route = routed`, and `active_kv_fallback_count = 0`, drops aggregate `cache_materialize_call_count` from `5379` to `3`, `cache_materialize_total_us` from `348621` to `278`, and `cache_update_total_us` from `1081676` to `728859`, but the release gate remains failed because `worker_tps_overhead_pct = 61.43`.
- the fused decode-quantize experiment evidence is `docs/metrics/phase2-active-kv-vendored-turboquant-fused-quantize-experiment.json`; it validates a single-dispatch key/value q4 affine quantizer including bfloat16 output handling, but the real-model data rejects it for default runtime use because aggregate `cache_quantize_total_us` regressed from the storage-fastpath `92171` to `823275`. The experimental route is therefore gated behind `MELIX_SWIFT_TURBOQUANT_FUSED_QUANTIZE=1`, and default `QuantizedKVCache.updateQuantizedStorage(...)` keeps using MLX's native vectorized `quantized(...)` implementation.
- the append-slice speedup evidence is `docs/metrics/phase2-active-kv-vendored-turboquant-append-slice.json`; it shortens q4 storage writes from `.ellipsis` plus trailing full-slice indexing to direct 3-axis slice updates. The same real-model gate keeps `active_kv_kernel_path = tq_mse_single`, `active_kv_runtime_route = routed`, `active_kv_fallback_count = 0`, and `active_kv_estimated_memory_savings_pct = 75.0`; aggregate `cache_append_total_us` improves from `615953` to `592356` and `cache_update_total_us` from `728859` to `725794`, but the fused release gate remains failed because `worker_tps_overhead_pct = 60.0`.

## Verification

- `make swift-test`
- `make integration-test`
- `xcrun swift test --package-path services/mlx-text-worker-swift --filter 'WorkerScaffoldTests/testVendoredFusedQ4AttentionMatchesQuantizedReferenceForDecodeGQA|WorkerScaffoldTests/testAttentionWithCacheUpdateUsesFusedQuantizedStorageWithoutMaterializingForDecode|WorkerScaffoldTests/testTurboQuantRuntimeRouteReportsRoutedAfterFusedAttentionDispatch|WorkerScaffoldTests/testAutoSwiftMLXBackendDecodeReportsTurboQuantFusedRuntimeRoute'`
- `MELIX_SWIFT_MLX_METALLIB_PATH=/path/to/mlx_metal-0.29.1/mlx.metallib MELIX_DEV_TEXT_MODEL_PATH=/path/to/mlx-community/Qwen3-0.6B-4bit bash scripts/dev_up.sh --prefer-built`; keep the Swift worker metallib aligned with `mlx-swift` 0.29.1 because newer Python `mlx_metal` metallibs can fail real-model RoPE specialization before the TurboQuant probe runs
- `PYTHONPATH="$(pwd):$(pwd)/services/mlx-worker-python" UV_CACHE_DIR="$(pwd)/.uv-cache" uv run --project services/mlx-worker-python python scripts/phase2_metrics_report.py --json --runtime-dir "$MELIX_RUNTIME_DIR" --http-prompt "Continue this sentence with five short words: Melix measures cache speed by" --model-path /path/to/mlx-community/Qwen3-0.6B-4bit --model-revision main --decode-repeats 5 --active-kv-profiles q4 --skip-abort --output docs/metrics/phase2-affine-q4-preopt.json`
- `PYTHONPATH="$(pwd):$(pwd)/services/mlx-worker-python" UV_CACHE_DIR="$(pwd)/.uv-cache" uv run --project services/mlx-worker-python --extra mlx python scripts/phase2_metrics_report.py --json --runtime-dir "$MELIX_RUNTIME_DIR" --http-prompt "Continue this sentence with five short words: Melix measures cache speed by" --model-path /path/to/mlx-community/Qwen3-0.6B-4bit --model-revision main --decode-repeats 5 --active-kv-profiles q4,turboquant-q4 --skip-abort --output docs/metrics/phase2-active-kv-decode-guard-postopt.json`
- `PYTHONPATH="$(pwd):$(pwd)/services/mlx-worker-python" UV_CACHE_DIR="$(pwd)/.uv-cache" uv run --project services/mlx-worker-python --extra mlx python scripts/phase2_metrics_report.py --json --runtime-dir "$MELIX_RUNTIME_DIR" --http-prompt "Continue this sentence with five short words: Melix measures cache speed by" --model-path /path/to/mlx-community/Qwen3-0.6B-4bit --model-revision main --decode-repeats 5 --active-kv-profiles q4,turboquant-q4 --skip-abort --output docs/metrics/phase2-active-kv-runtime-speedup-postopt.json`
- `PYTHONPATH="$(pwd):$(pwd)/services/mlx-worker-python" UV_CACHE_DIR="$(pwd)/.uv-cache" uv run --project services/mlx-worker-python --extra mlx python scripts/phase2_metrics_report.py --json --runtime-dir "$MELIX_RUNTIME_DIR" --http-prompt "Continue this sentence with five short words: Melix measures cache speed by" --model-path /path/to/mlx-community/Qwen3-0.6B-4bit --model-revision main --decode-repeats 5 --active-kv-profiles q4,turboquant-q4 --skip-abort --output docs/metrics/phase2-active-kv-candidate-check-postopt.json`
- `PYTHONPATH="$(pwd):$(pwd)/services/mlx-worker-python" UV_CACHE_DIR="$(pwd)/.uv-cache" uv run --project services/mlx-worker-python --extra mlx python scripts/phase2_metrics_report.py --json --runtime-dir "$MELIX_RUNTIME_DIR" --http-prompt "Continue this sentence with five short words: Melix measures cache speed by" --model-path /path/to/mlx-community/Qwen3-0.6B-4bit --model-revision main --decode-repeats 5 --active-kv-profiles q4,turboquant-q4 --skip-abort --output docs/metrics/phase2-active-kv-terminal-model-call-postopt.json`
- `PYTHONPATH="$(pwd):$(pwd)/services/mlx-worker-python" UV_CACHE_DIR="$(pwd)/.uv-cache" uv run --project services/mlx-worker-python --extra mlx python scripts/phase2_metrics_report.py --json --runtime-dir "$MELIX_RUNTIME_DIR" --http-prompt "Continue this sentence with five short words: Melix measures cache speed by" --model-path /path/to/mlx-community/Qwen3-0.6B-4bit --model-revision main --decode-repeats 5 --active-kv-profiles q4,turboquant-q4 --skip-abort --output docs/metrics/phase2-active-kv-lazy-eval-probe.json`
- `PYTHONPATH="$(pwd):$(pwd)/services/mlx-worker-python" UV_CACHE_DIR="$(pwd)/.uv-cache" uv run --project services/mlx-worker-python --extra mlx python scripts/phase2_metrics_report.py --json --runtime-dir "$MELIX_RUNTIME_DIR" --http-prompt "Continue this sentence with five short words: Melix measures cache speed by" --model-path /path/to/mlx-community/Qwen3-0.6B-4bit --model-revision main --decode-repeats 5 --active-kv-profiles q4,turboquant-q4 --skip-abort --output docs/metrics/phase2-active-kv-blocked-fallback-speedup-postopt.json`
- `MELIX_SWIFT_TURBOQUANT_CANDIDATE_PROBE=1` must be set before starting `scripts/dev_up.sh` for candidate-audit runs; then run `PYTHONPATH="$(pwd):$(pwd)/services/mlx-worker-python" UV_CACHE_DIR="$(pwd)/.uv-cache" uv run --project services/mlx-worker-python --extra mlx python scripts/phase2_metrics_report.py --json --runtime-dir "$MELIX_RUNTIME_DIR" --http-prompt "Continue this sentence with five short words: Melix measures cache speed by" --model-path /path/to/mlx-community/Qwen3-0.6B-4bit --model-revision main --decode-repeats 5 --active-kv-profiles q4,turboquant-q4 --skip-abort --require-fused-turboquant --output docs/metrics/phase2-active-kv-fused-turboquant-candidate.json`
- `PYTHONPATH="$(pwd):$(pwd)/services/mlx-worker-python" UV_CACHE_DIR="$(pwd)/.uv-cache" uv run --project services/mlx-worker-python --extra mlx python scripts/phase2_metrics_report.py --input-json docs/metrics/phase2-active-kv-decode-guard-postopt.json --json --require-fused-turboquant --output docs/metrics/phase2-active-kv-fused-turboquant-candidate.json`; current fallback evidence is expected to write the output and then exit non-zero
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-tq MELIX_HTTP_PORT=11435 MELIX_SWIFT_MLX_METALLIB_PATH=/tmp/melix-mlx-0.29.1/mlx/lib/mlx.metallib MELIX_DEV_TEXT_MODEL_PATH=/path/to/mlx-community/Qwen3-0.6B-4bit bash scripts/dev_up.sh --prefer-built`; this is the real-model stack used for the vendored runtime probe
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-tq make phase2-metrics PHASE2_METRICS_ARGS='--model-id melix-dev-text --model-path /path/to/mlx-community/Qwen3-0.6B-4bit --model-revision main --active-kv-profiles turboquant-q4 --decode-repeats 3 --skip-abort --json --output docs/metrics/phase2-active-kv-vendored-turboquant-runtime.json'`
- `make phase2-metrics PHASE2_METRICS_ARGS='--input-json docs/metrics/phase2-active-kv-vendored-turboquant-runtime.json --json --require-fused-turboquant'`; current vendored evidence is expected to exit non-zero with `worker_tps_overhead_pct=68.63`
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-tq-shared MELIX_HTTP_PORT=11436 MELIX_SWIFT_MLX_METALLIB_PATH=/tmp/melix-mlx-0.29.1/mlx/lib/mlx.metallib MELIX_DEV_TEXT_MODEL_PATH=/path/to/mlx-community/Qwen3-0.6B-4bit bash scripts/dev_up.sh --prefer-built`; this is the real-model stack used for the shared-score speedup probe
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-tq-shared make phase2-metrics PHASE2_METRICS_ARGS='--model-id melix-dev-text --model-path /path/to/mlx-community/Qwen3-0.6B-4bit --model-revision main --active-kv-profiles turboquant-q4 --decode-repeats 3 --skip-abort --json --output docs/metrics/phase2-active-kv-vendored-turboquant-shared-scores.json'`
- `make phase2-metrics PHASE2_METRICS_ARGS='--input-json docs/metrics/phase2-active-kv-vendored-turboquant-shared-scores.json --json --require-fused-turboquant'`; current shared-score evidence is expected to exit non-zero with `worker_tps_overhead_pct=60.87`
- `xcrun swift test --package-path services/mlx-text-worker-swift --filter 'WorkerScaffoldTests/testVendoredFusedQ4AttentionLaunchPlanUsesOnlineSoftmaxAcrossValueLanes|WorkerScaffoldTests/testVendoredFusedQ4AttentionMatchesQuantizedReferenceForDecodeGQA'`
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-tq-online MELIX_HTTP_PORT=11436 MELIX_SWIFT_MLX_METALLIB_PATH=/tmp/melix-mlx-0.29.1/mlx/lib/mlx.metallib MELIX_DEV_TEXT_MODEL_PATH=/path/to/mlx-community/Qwen3-0.6B-4bit bash scripts/dev_up.sh --prefer-built`; this is the real-model stack used for the online-softmax speedup probe
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-tq-online make phase2-metrics PHASE2_METRICS_ARGS='--model-id melix-dev-text --model-path /path/to/mlx-community/Qwen3-0.6B-4bit --model-revision main --active-kv-profiles turboquant-q4 --decode-repeats 1 --skip-abort --json --output docs/metrics/phase2-active-kv-vendored-turboquant-online-softmax.json'`
- `make phase2-metrics PHASE2_METRICS_ARGS='--input-json docs/metrics/phase2-active-kv-vendored-turboquant-online-softmax.json --json --require-fused-turboquant'`; current online-softmax evidence is expected to exit non-zero with `worker_tps_overhead_pct=60.0`
- `swift test --package-path services/mlx-text-worker-swift --filter 'WorkerScaffoldTests/testQuantizedKVCacheRecordsUpdateAndMaterializeProbeTimings|WorkerScaffoldTests/testMetricsStoreTracksCountersAndTimings|WorkerScaffoldTests/testDecodeStreamingRpcRecordsActiveKVProbeSummary|WorkerScaffoldTests/testActiveKVProbeSummaryAveragesReturnZeroWithoutDecodeTokens'`
- `PYTHONPATH="$(pwd):$(pwd)/services/mlx-worker-python" uv run --project services/mlx-worker-python pytest services/mlx-worker-python/tests/test_phase2_metrics_report.py -k 'active_kv or repeats_decode_profiles'`
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-tq-cache-probe MELIX_HTTP_PORT=11444 MELIX_SWIFT_MLX_METALLIB_PATH=/tmp/melix-mlx-0.29.1/mlx/lib/mlx.metallib MELIX_DEV_TEXT_MODEL_PATH=/path/to/mlx-community/Qwen3-0.6B-4bit bash scripts/dev_up.sh --prefer-built`; this is the real-model stack used for the cache-internal timing probe
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-tq-cache-probe make phase2-metrics PHASE2_METRICS_ARGS='--model-id melix-dev-text --model-path /path/to/mlx-community/Qwen3-0.6B-4bit --model-revision main --active-kv-profiles turboquant-q4 --decode-repeats 3 --skip-abort --json --output docs/metrics/phase2-active-kv-vendored-turboquant-cache-probe.json'`
- `make phase2-metrics PHASE2_METRICS_ARGS='--input-json docs/metrics/phase2-active-kv-vendored-turboquant-cache-probe.json --json --require-fused-turboquant'`; current cache-internal timing evidence is expected to exit non-zero with `worker_tps_overhead_pct=73.61`
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-tq-storage-fastpath MELIX_HTTP_PORT=11445 MELIX_SWIFT_MLX_METALLIB_PATH=/tmp/melix-mlx-0.29.1/mlx/lib/mlx.metallib MELIX_DEV_TEXT_MODEL_PATH=/path/to/mlx-community/Qwen3-0.6B-4bit bash scripts/dev_up.sh --prefer-built`; this is the real-model stack used for the storage-fastpath speedup probe
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-tq-storage-fastpath make phase2-metrics PHASE2_METRICS_ARGS='--model-id melix-dev-text --model-path /path/to/mlx-community/Qwen3-0.6B-4bit --model-revision main --active-kv-profiles turboquant-q4 --decode-repeats 3 --skip-abort --json --output docs/metrics/phase2-active-kv-vendored-turboquant-storage-fastpath.json'`
- `make phase2-metrics PHASE2_METRICS_ARGS='--input-json docs/metrics/phase2-active-kv-vendored-turboquant-storage-fastpath.json --json --require-fused-turboquant'`; current storage-fastpath evidence is expected to exit non-zero with `worker_tps_overhead_pct=61.43`
- `MELIX_SWIFT_TURBOQUANT_FUSED_QUANTIZE=1 MELIX_RUNTIME_DIR=/tmp/melix-phase2-tq-fused-quantize MELIX_HTTP_PORT=11446 MELIX_SWIFT_MLX_METALLIB_PATH=/tmp/melix-mlx-0.29.1/mlx/lib/mlx.metallib MELIX_DEV_TEXT_MODEL_PATH=/path/to/mlx-community/Qwen3-0.6B-4bit bash scripts/dev_up.sh --prefer-built`; this is the rejected experimental fused decode-quantize stack
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-tq-fused-quantize make phase2-metrics PHASE2_METRICS_ARGS='--model-id melix-dev-text --model-path /path/to/mlx-community/Qwen3-0.6B-4bit --model-revision main --active-kv-profiles turboquant-q4 --decode-repeats 3 --skip-abort --json --output docs/metrics/phase2-active-kv-vendored-turboquant-fused-quantize-experiment.json'`
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-tq-append-slice MELIX_HTTP_PORT=11446 MELIX_SWIFT_MLX_METALLIB_PATH=/tmp/melix-mlx-0.29.1/mlx/lib/mlx.metallib MELIX_DEV_TEXT_MODEL_PATH=/path/to/mlx-community/Qwen3-0.6B-4bit bash scripts/dev_up.sh --prefer-built`; this is the real-model stack used for the append-slice speedup probe
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-tq-append-slice make phase2-metrics PHASE2_METRICS_ARGS='--model-id melix-dev-text --model-path /path/to/mlx-community/Qwen3-0.6B-4bit --model-revision main --active-kv-profiles turboquant-q4 --decode-repeats 3 --skip-abort --json --output docs/metrics/phase2-active-kv-vendored-turboquant-append-slice.json'`
- `make phase2-metrics PHASE2_METRICS_ARGS='--input-json docs/metrics/phase2-active-kv-vendored-turboquant-append-slice.json --json --require-fused-turboquant'`; current append-slice evidence is expected to exit non-zero with `worker_tps_overhead_pct=60.0`
- `MELIX_RUNTIME_DIR=/tmp/melix-qwen35-live MELIX_HTTP_PORT=11442 MELIX_SWIFT_MLX_METALLIB_PATH=/tmp/melix-mlx-0.29.1/mlx/lib/mlx.metallib MELIX_DEV_TEXT_MODEL_PATH=/path/to/mlx-community/Qwen3.5-0.8B-OptiQ-4bit bash scripts/dev_up.sh --prefer-built`; this is the real-model stack used for the Qwen3.5 support smoke
- `MELIX_RUNTIME_DIR=/tmp/melix-qwen35-live make phase2-metrics PHASE2_METRICS_ARGS='--model-id melix-dev-text --model-path /path/to/mlx-community/Qwen3.5-0.8B-OptiQ-4bit --model-revision main --active-kv-profiles turboquant-q4 --decode-repeats 1 --skip-abort --json --output docs/metrics/phase2-active-kv-qwen35-support-smoke.json'`
- `make phase2-metrics PHASE2_METRICS_ARGS='--input-json docs/metrics/phase2-active-kv-qwen35-support-smoke.json --json --require-fused-turboquant'`; current Qwen3.5 evidence is expected to exit non-zero with `active_kv_runtime_block_reason=unsupported_cache_state`
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-tq-eval-probe MELIX_HTTP_PORT=11447 MELIX_SWIFT_MLX_METALLIB_PATH=/tmp/melix-mlx-0.29.1/mlx/lib/mlx.metallib MELIX_DEV_TEXT_MODEL_PATH=/path/to/mlx-community/Qwen3.5-0.8B-OptiQ-4bit MELIX_SWIFT_ACTIVE_KV_FORCE_MODEL_EVAL_PROBE=1 python3 scripts/dev_up.py --prefer-built`; this is the real-model stack used for the fused eval-sync probe
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-tq-eval-probe make phase2-metrics PHASE2_METRICS_ARGS='--real-small-model --active-kv-profiles turboquant-q4 --decode-repeats 3 --skip-abort --json --output docs/metrics/phase2-active-kv-vendored-turboquant-eval-probe.json'`
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-tq-eval-probe make phase2-metrics PHASE2_METRICS_ARGS='--input-json docs/metrics/phase2-active-kv-vendored-turboquant-eval-probe.json --json --require-fused-turboquant'`; current Qwen3.5 eval-sync evidence is expected to exit non-zero with `active_kv_runtime_block_reason=unsupported_cache_state`
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-qwen35-hybrid-route MELIX_HTTP_PORT=11448 MELIX_SWIFT_MLX_METALLIB_PATH=/tmp/melix-mlx-0.29.1/mlx/lib/mlx.metallib MELIX_DEV_TEXT_MODEL_PATH=/path/to/mlx-community/Qwen3.5-0.8B-OptiQ-4bit MELIX_SWIFT_ACTIVE_KV_FORCE_MODEL_EVAL_PROBE=1 python3 scripts/dev_up.py --prefer-built`; this is the real-model stack used for the Qwen3.5 hybrid-cache routed probe
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-qwen35-hybrid-route make phase2-metrics PHASE2_METRICS_ARGS='--real-small-model --active-kv-profiles turboquant-q4 --decode-repeats 3 --skip-abort --json --output docs/metrics/phase2-active-kv-qwen35-hybrid-turboquant-routing.json'`
- `MELIX_RUNTIME_DIR=/tmp/melix-phase2-qwen35-hybrid-route make phase2-metrics PHASE2_METRICS_ARGS='--input-json docs/metrics/phase2-active-kv-qwen35-hybrid-turboquant-routing.json --json --require-fused-turboquant'`; the current Qwen3.5 hybrid-cache routed evidence exits zero
- `docs/metrics/phase2-active-kv-qwen35-hybrid-stability-summary.json` summarizes five repeated Qwen3.5 hybrid-cache runs generated under `/tmp/melix-phase2-qwen35-hybrid-stability`; each raw run was checked with `--require-fused-turboquant`
- inspect `swift_worker_direct.decode[]` for `label = "decode_affine_q4"`
- inspect `swift_worker_direct.decode[]` for `label = "decode_turboquant_q4"`; `active_kv_candidate_dispatch_code = 1` only proves a candidate dispatch ran, while `active_kv_kernel_path != "fallback"` is required before claiming fused TurboQuant
- inspect `docs/metrics/phase2-active-kv-runtime-speedup-postopt.json` for `candidate_dispatch_count = 0` on the default path
- inspect `docs/metrics/phase2-active-kv-candidate-check-postopt.json` for `candidate_eligibility_check_count = 0` on the default path
- inspect `docs/metrics/phase2-active-kv-terminal-model-call-postopt.json` for `decode_model_call_count = 315` on the default `turboquant-q4` release gate
- inspect `docs/metrics/phase2-active-kv-lazy-eval-probe.json` for `decode_token_eval_total_us = 6438631` and `decode_loop_total_us = 9497000` on the default `turboquant-q4` release gate
- inspect `docs/metrics/phase2-active-kv-blocked-fallback-speedup-postopt.json` for default `turboquant-q4` `worker_tps_overhead_pct = -3.39`, `active_kv_estimated_memory_savings_pct = 0.0`, and `status = "fail"` while the hook is blocked
- inspect `docs/metrics/phase2-active-kv-vendored-turboquant-runtime.json` for `active_kv_kernel_path = tq_mse_single`, `active_kv_runtime_route = routed`, `active_kv_fallback_count = 0`, `active_kv_decode_quantize_total_us = 0`, `active_kv_estimated_memory_savings_pct = 75.0`, and `worker_tps_overhead_pct = 68.63`
- inspect `docs/metrics/phase2-active-kv-vendored-turboquant-shared-scores.json` for `active_kv_kernel_path = tq_mse_single`, `active_kv_runtime_route = routed`, `active_kv_fallback_count = 0`, `active_kv_decode_quantize_total_us = 0`, `active_kv_estimated_memory_savings_pct = 75.0`, and `worker_tps_overhead_pct = 60.87`
- inspect `docs/metrics/phase2-active-kv-vendored-turboquant-online-softmax.json` for `active_kv_kernel_path = tq_mse_single`, `active_kv_runtime_route = routed`, `active_kv_fallback_count = 0`, `active_kv_decode_quantize_total_us = 0`, `active_kv_estimated_memory_savings_pct = 75.0`, and `worker_tps_overhead_pct = 60.0`; the gate must remain failed until a later same-model JSON reports `worker_tps_overhead_pct <= 15`
- inspect `docs/metrics/phase2-active-kv-vendored-turboquant-packed-word-lanes.json` for `active_kv_kernel_path = tq_mse_single`, `active_kv_runtime_route = routed`, `active_kv_fallback_count = 0`, `active_kv_decode_quantize_total_us = 0`, `active_kv_estimated_memory_savings_pct = 75.0`, `decode_token_eval_total_us = 2554186`, and `worker_tps_overhead_pct = 73.97`; the gate must remain failed until a later same-model JSON reports `worker_tps_overhead_pct <= 15`
- inspect `docs/metrics/phase2-active-kv-vendored-turboquant-cache-probe.json` for `cache_update_total_us = 1081676`, `cache_append_total_us = 621474`, `cache_quantize_total_us = 93119`, `cache_materialize_total_us = 348621`, `active_kv_cache_update_avg_us = 200.0`, `active_kv_cache_materialize_avg_us = 64.0`, and `worker_tps_overhead_pct = 73.61`; the probe explains overhead but must not pass the fused release gate by itself
- inspect `docs/metrics/phase2-active-kv-vendored-turboquant-storage-fastpath.json` for `cache_update_total_us = 728859`, `cache_append_total_us = 615953`, `cache_quantize_total_us = 92171`, `cache_materialize_total_us = 278`, `cache_materialize_call_count = 3`, `active_kv_cache_update_avg_us = 135.0`, `active_kv_cache_materialize_avg_us = 93.0`, and `worker_tps_overhead_pct = 61.43`; the materialization fast path improves overhead but must not pass the fused release gate by itself
- inspect `docs/metrics/phase2-active-kv-vendored-turboquant-fused-quantize-experiment.json` for the rejected experimental route: `cache_quantize_total_us = 823275`, `cache_update_total_us = 1575930`, `active_kv_kernel_path = tq_mse_single`, `active_kv_fallback_count = 0`, and `worker_tps_overhead_pct = 56.25`; the path is correctness-covered but must stay opt-in because it regresses native quantization cost
- inspect `docs/metrics/phase2-active-kv-vendored-turboquant-append-slice.json` for `cache_update_total_us = 725794`, `cache_append_total_us = 592356`, `cache_quantize_total_us = 112194`, `cache_materialize_total_us = 304`, `cache_materialize_call_count = 3`, and `worker_tps_overhead_pct = 60.0`; the append slice cleanup is measurable but must not pass the fused release gate by itself
- inspect `docs/metrics/phase2-active-kv-qwen35-support-smoke.json` for successful Swift load/decode and for the current Qwen3.5 release-gate blocker: `active_kv_kernel_path = fallback`, `active_kv_runtime_route = blocked`, `active_kv_runtime_block_reason = unsupported_cache_state`, `active_kv_fallback_count = 1`, and `active_kv_estimated_memory_savings_pct = 0.0`
- inspect `docs/metrics/phase2-active-kv-vendored-turboquant-eval-probe.json` for Qwen3.5 forced model-eval sync evidence: `decode_model_eval_sync_total_us = 4311359`, `decode_model_eval_sync_call_count = 189`, `active_kv_fused_attention_call_count = 0`, `active_kv_kernel_path = fallback`, and `status = "fail"`
- inspect `docs/metrics/phase2-active-kv-qwen35-hybrid-turboquant-routing.json` for Qwen3.5 fused release-gate pass evidence: `active_kv_kernel_path = tq_mse_single`, `active_kv_runtime_route = routed`, `active_kv_fallback_count = 0`, `active_kv_estimated_memory_savings_pct = 75.0`, `fused_attention_call_count = 1134`, `worker_tps_overhead_pct = 12.82`, and `status = "pass"`
- inspect `docs/metrics/phase2-active-kv-qwen35-hybrid-stability-summary.json` for repeated-run Qwen3.5 fused release-gate evidence: `pass_count = 5`, `run_count = 5`, `all_non_fallback = true`, `all_routed = true`, `all_worker_overhead_within_threshold = true`, and maximum `worker_tps_overhead_pct = 12.82`
- inspect `docs/metrics/phase2-active-kv-qwen35-turboquant-speedup-preopt.json` and `docs/metrics/phase2-active-kv-qwen35-turboquant-speedup-postopt.json` for the fused-kernel lane work reduction: active/launched lanes stay `12096`, inactive lanes stay `0`, softmax token-lane work drops from `641088` to `20034`, and the post-run release gate reports `status = "pass"` with `worker_tps_overhead_pct = 5.13`
- inspect `docs/metrics/phase2-active-kv-qwen35-turboquant-speedup-stability-summary.json` before using the speedup as repeated-run pass evidence; current warmup-aware summary reports `pass_count = 5`, `run_count = 5`, `all_non_fallback = true`, `all_routed = true`, and maximum `worker_tps_overhead_pct = 12.5`
- inspect `swift_worker_direct.active_kv_release_gates.turboquant_fused_decode`; current fallback probes should report `status = "fail"`, while fused candidates must report `status = "pass"`
- inspect `swift_worker_direct.active_kv_fused_candidate_probes.turboquant_q4`; current fallback probes should report `status = "runtime_blocked"`
- inspect `swift_worker_direct.comparisons.affine_q4_vs_baseline` for throughput overhead, TTFT delta, quantization share, and estimated memory savings
- inspect `swift_worker_direct.comparisons.turboquant_q4_vs_baseline.worker_tps_overhead_pct`; values above 15 percent must keep the fused gate failed
- preserve the emitted JSON as the affine q4 pre-optimization baseline before making TurboQuant kernel changes
- run `xcrun swift test --package-path services/mlx-text-worker-swift --filter WorkerScaffoldTests/testTurboQuantMetalCapabilityRuns` and `xcrun swift test --package-path services/mlx-text-worker-swift --filter WorkerScaffoldTests/testTurboQuantCandidateDispatchReadsQuantizedKVCacheState` after custom Metal changes; the tests require a discoverable local MLX `mlx.metallib` and use the existing temporary `default.metallib` fixture

## Acceptance

- KV-cache quantization acceleration can be enabled through explicit policy
- memory and throughput effects are measurable and benchmarked
- optimization work is blocked until the report contains non-`N/A` baseline and affine q4 decode rows plus an affine-vs-baseline comparison
- any later TurboQuant profile must be compared against both baseline and affine q4 using the same probe fields
- true TurboQuant remains incomplete until a post-run JSON shows the TurboQuant profile on a non-fallback fused kernel path with measured throughput overhead improvement
- true TurboQuant remains blocked until `--require-fused-turboquant` exits zero on the fused candidate report
