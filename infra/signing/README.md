# Signing Structure

This directory reserves the repository-owned location for Melix signing and notarization material.

Phase 8 milestone `P8-M4` does not ship production signing credentials.
It does define the repository location where future signing configuration, guidance, and release-time assets will live.
